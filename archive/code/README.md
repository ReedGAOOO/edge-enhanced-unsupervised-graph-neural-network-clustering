# Archived Code

This folder keeps helper scripts and experiment drivers that are no longer part of the active top-level tool surface.

- `historical_tools/`: older Python experiment runners and comparison helpers
- `historical_scripts/`: older shell wrappers
- `historical_builders/`: legacy dataset-conversion or builder scripts no longer kept at the top-level `tools/`
